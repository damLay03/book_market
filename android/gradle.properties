org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Kotlin incremental caches cannot relativize sources when the project and the
# global Pub cache live on different Windows drives. Disabling them keeps local
# and CI builds deterministic; Gradle build caching still remains available.
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
